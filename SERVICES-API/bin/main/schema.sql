CREATE TABLE `TEAM_DETAILS` (
	`ID1` INT NOT NULL AUTO_INCREMENT,
	`EMPID` VARCHAR(255) NOT NULL UNIQUE,
	`EMPNAME` VARCHAR(255) NOT NULL,
	`DESIGNATION` VARCHAR(255) NOT NULL,
	`TEAM_NAME` VARCHAR(255) NOT NULL,
	`BU` VARCHAR(255) NOT NULL,
	PRIMARY KEY (`ID1`,`EMPID`)
);

CREATE TABLE `TEAM1` (
	`ID2` INT NOT NULL AUTO_INCREMENT,
	`TEAM_NAME` VARCHAR(255) NOT NULL,
	`ID1` INT NOT NULL,
	PRIMARY KEY (`ID2`)
);

CREATE TABLE `TEAM2` (
	`ID3` BINARY NOT NULL,
	`TEAM_NAME` VARCHAR(255) NOT NULL,
	`ID1` INT NOT NULL,
	PRIMARY KEY (`ID3`)
);

ALTER TABLE `TEAM1` ADD CONSTRAINT `TEAM1_fk0` FOREIGN KEY (`ID1`) REFERENCES `TEAM_DETAILS`(`ID1`);

ALTER TABLE `TEAM2` ADD CONSTRAINT `TEAM2_fk0` FOREIGN KEY (`ID1`) REFERENCES `TEAM_DETAILS`(`ID1`);
